info:
  name: create usuário
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/clientes
  body:
    type: json
    data: |-
      {
        "nome": "vinicius",
        "email": "vinicius@gmail.com",
        "telefone": "48999999999"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
