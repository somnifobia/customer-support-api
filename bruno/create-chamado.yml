info:
  name: create chamado
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/chamados
  body:
    type: json
    data: |-
      {
        "titulo": "Erro ao acessar sistema",
        "descricao": "O cliente não consegue fazer login.",
        "status": "ABERTO",
        "cliente": {
          "id": 1
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
